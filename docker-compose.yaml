version: '3'
services:
  data_provider:
    build:
      context: ./
      dockerfile: ./build/Dockerfile
    image: allensyk/kaiji-data-provider:1.0.0
    container_name: provider
    command: ['sleep', 'infinity']
